<control:MetroWindow x:Class="AirMonitor.ShellView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AirMonitor"
             xmlns:control="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:ctrl="clr-namespace:AirMonitor.Controls"
             Name="Main"
                     
             mc:Ignorable="d" Width="1280" Height="720">
    <Grid>
        <ContentControl Content="{Binding MainContent}"></ContentControl>
        <CheckBox Name="Open"  />
        <Popup Placement="Bottom" ctrl:PopupHelper.AttachWindow="{Binding ElementName=Main}" IsOpen="{Binding ElementName=Open,Path=IsChecked}"
               AllowsTransparency="True"
               VerticalOffset="{Binding ElementName=BottomPanel,Path=ActualHeight,Converter={StaticResource Multiple} ,ConverterParameter=-1}">
            <Grid Name="BottomPanel" Width="{Binding ElementName=Main,Path=ActualWidth}" Background="Red" Height="100">

            </Grid>
        </Popup>

    </Grid>
</control:MetroWindow>
